import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.samesci-payroll-hub.app',
  appName: 'SamesCI-Payroll-Hub',
  webDir: 'dist'
};

export default config;
